// Styled
import { HoverButton } from "./styled";

const AddSection: React.FC = () => {
  return (
    <HoverButton type="button" className="hover_action_button">
      Add section
    </HoverButton>
  );
};
export default AddSection;
